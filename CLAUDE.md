# 먹점프 (Muk Jump) — 동양화 점프킹 프로젝트

> Unity 기반 모바일 캐주얼 드로잉 클라이밍 게임
> Apple App Store · Google Play · Apps in Toss 출시 준비

이 문서는 Claude Code와 Codex가 프로젝트 작업을 시작할 때 참고하는 최신 컨텍스트 파일이다.
현재 Unity 코어 루프, 드로잉 발판, 장애물, 아이템 4종, 먹분신, 사망 연출까지 구현되어
`main`에 병합된 상태다. 아래 11절의 현재 저장소 상태를 기준으로 후속 작업을 이어간다.

---

## 작업 시작 시 필수 절차

Claude Code를 포함한 모든 코딩 에이전트는 다음 순서를 지킨다.

1. 루트의 `AGENTS.md`와 이 `CLAUDE.md`를 끝까지 읽는다.
2. `git status --short --branch`로 현재 브랜치와 다른 작업자의 미커밋 변경을 확인한다.
3. `main` 하나에서 직접 작업한다. 별도 브랜치·clone·worktree는 만들지 않는다.
4. 구현에 앞서 관련 스크립트와 `MukJumpSceneBuilder.cs`의 현재 연결 구조를 확인한다.
5. 기능 구현 후 Unity 컴파일 로그와 변경 파일을 검증한다.

### 사용자와 합의된 기본 작업 흐름

- 기능 단위로 구현하고 완료·검증한 작업은 바로 커밋한다(2026-09-10 사용자 지시).
  커밋 승인을 매번 묻지 않으며 push는 별도 요청/승인 범위에서 수행한다.
- UI·게임·문서를 하나의 Unity 프로젝트에서 연속 작업하며 별도 브랜치와 자동 머지는 사용하지 않는다.
- `git add -A`와 `git add .`은 사용하지 않고 이번 작업에서 수정한 파일만 명시적으로 추가한다.
- 다른 에이전트의 미커밋 변경은 커밋·리셋·체크아웃하지 않는다.
- 릴리스·배포·외부 게시처럼 외부 상태를 바꾸는 작업은 별도로 확인한다.

### Unity와 원격 조작 원칙

- `Assets/Scenes/Main.unity`를 YAML이나 Inspector에서 직접 편집하지 않는다.
  `Assets/Editor/MukJumpSceneBuilder.cs`를 수정하고 `MukJump > Build Main Scene`으로 재생성한다.
- Unity 에디터, Chrome, ChatGPT 등 GUI 원격 조작은 사용자가 “원격으로 해줘”라고
  명시했을 때만 수행한다. 그 외에는 코드와 에셋만 수정하고 필요한 메뉴 실행을 안내한다.
- Unity 에디터가 열려 있으면 같은 프로젝트를 `-batchmode`로 실행하지 않는다.
- 스크립트 수정 후 `~/Library/Logs/Unity/Editor.log`에서 `error CS`와 `Exception`을
  확인한 뒤 테스트를 요청한다.
- Input System 전용 프로젝트이므로 구형 `Input.*` 대신 `MukJump.Core.PointerInput`을 쓴다.
- 싱글톤은 Play 중 재컴파일을 고려해 `Instance`를 `OnEnable`에서 다시 할당한다.

### 문서 동기화 원칙

- 게임 규칙이나 구현 상태가 바뀌면 같은 작업에서 `README.md`, `docs/project-brief.md`,
  이 `CLAUDE.md` 중 영향을 받는 문서를 함께 갱신한다.
- `README.md`는 GitHub 첫 화면의 플레이어용 게임 설명서다. 기획·밸런스·콘텐츠·조작·
  성장 규칙이 바뀌면 해당 기능 커밋에서 반드시 최신 요약으로 함께 수정하고, 개발용
  내부 구현 세부는 `docs/` 문서로 분리한다.
- 작업 과정 기록은 필수가 아니다. 외부 에셋을 추가하면 출처와 라이선스는 보존한다.

## 0. 제품 기본 정보

- 세계 순위 국기는 기기의 국가·지역(`deviceRegion`)을 표시한다. iOS NSLocale의 countryCode,
  Android Locale의 country를 읽으며 언어·GPS·IP와 무관하다. 미확인/기존 누락 행은 지구 아이콘이다.
  뒤끝 리더보드 추가 항목은 `deviceRegion` 열에 연결해야 한다. 국가 변경은 계정·점수를 초기화하지 않는다.

- 제품명: 먹점프
- 배포 대상: Apple App Store · Google Play · Apps in Toss 일반 서비스
- 팀 구성: 2인 (김승연 + 최성빈) — Team-Swooord
- GitHub 레포: https://github.com/Team-Swooord/muk-jump (Public)
- 번들 ID 기본값: `com.CYSB.MukJump`

## 1. 스토어 출시 기준

- iOS·Android 네이티브와 Apps in Toss WebGL에서 동일한 코어 게임이 동작해야 한다.
- 노치·Dynamic Island·홈 인디케이터는 `Screen.safeArea`로 대응한다.
- 백그라운드 진입 즉시 게임과 소리를 멈추고, 복귀 시 해당 일시정지 소유자만 재개한다.
- Release 빌드에는 DEBUG UI와 플랫폼 실험 코드를 노출하지 않는다.
- 최고 기록과 영구 성장 데이터는 설치 단위 로컬 저장을 유지한다.
  iOS·Android는 첫 실행 때 뒤끝 게스트 계정(UUID 기반 ID)을 자동 생성하고, 다음 실행은
  저장된 토큰으로 같은 계정을 확인한다. 네이티브 Development Build도 같은 경로를 사용하며
  에디터 자동 테스트·토스 WebGL은 뒤끝에 접속하지 않는다. 성장·먹빛·최고 기록은 기존
  뒤끝 스냅샷에 동기화하고, 이번 로그인 테스트 배포의 계정 연동 UI는 Apple만 제공한다.
  Google 로그인 버튼은 생성하지 않고 런타임 진입도 잠근다. 기존 제공자 기록과 SDK는 보존한다.
  Apple 선택은 게스트 계정을 전환한다.
  다른 계정의 기존 기록은 확인 없이 덮어쓰지 않는다. 게스트 토큰 만료는 SDK에 기존
  게스트 자격 정보가 남았을 때만 재로그인하며, 소유자가 달라지면 저장을 중단한다.
  최초 연결 실패는 로컬 기록을 유지한 채 로비에서 5~60초 간격으로 재시도한다.
  계정 전환/삭제 복구 중·플레이 도중에는 자동 재연결하지 않는다. 정상 로그아웃·삭제 정리가
  끝나면 새 게스트 연결을 허용한다. 구버전의 자동 인증 억제 값은 소유자·복구 표식이 없는
  로컬 게스트에 한해 이관하고, 이전 게스트 인증 정보를 정리한 뒤 새로 연결한다.
  계정 창에는 실제 뒤끝 UID와 기기 변경 전 연동 안내를 표시한다. 게스트만으로는 앱 삭제·
  기기 변경 후 복구가 보장되지 않는다. iOS 설정은 존재하며 실제 인증 왕복 검증은 별도다.
- iOS·Android는 로비·플레이 공통 상단 적응형 배너와 게임오버 선택형 Google 보상 광고를 사용한다.
  토스·에디터도 같은 배너 노출 규칙을 쓰며, 일시정지·튜토리얼·전환·전체화면 광고에는
  숨긴다. 고도·풍향·일시정지 한지 띠는 실제 배너 하단에서 화면 10px 아래에 고정한다.
  처음 로드 전에는 광고 슬롯을 예약하되 실제 높이를 받으면 남는 공간을 돌려준다.
  로비의 예약 배치는 유지하고, 일시정지·no-fill에는 플레이 HUD의 마지막 기준선도 유지한다.
  광고 영역에서는 먹선을 그릴 수 없고 일시정지의 투명 터치 영역도 광고를 덮지 않는다.
  광고 부활은 판당 한 번 체력 1·50m 상승·방어막 1개를 지급한다.
  에디터와 Development Build는 Google 공식 테스트 ID만 사용하고, Release는 먹점프
  전용 앱·광고 단위의 게시자 일치와 검증 완료를 빌드 전에 강제한다.
- 외부 에셋의 스토어 배포·저장소 재배포 라이선스를 최종 확인한다.

## 2. 게임 한 줄 컨셉

> "선 하나가 발판이 되고, 발판 하나가 그림이 된다"

동양화(수묵화) 붓질로 그리는 발판 + 자동으로 오르는 캐릭터 + AI로 설계한 수묵 아트.
캐릭터는 조작할 수 없고 일정 주기마다 스스로 점프한다. 플레이어는 점프 타이밍에 맞춰
발판을 그려서 캐릭터의 다음 궤적(방향·높이)을 유도한다. 기존 '점프킹'이 점프력을
직접 조작하는 게임이라면, 이 게임은 발판을 그리는 행위 자체가 조작이 된다.

## 3. 게임 개요

| 항목 | 내용 |
|---|---|
| 장르 | 캐주얼 아케이드 클라이밍 (드로잉 플랫포머) |
| 플랫폼 | 모바일 iOS · Android · Apps in Toss WebGL, 세로 화면 |
| 엔진 | Unity (6000.5.9f1, Apple Silicon, Update) / URP, Universal 2D 템플릿 |
| 타겟 | 전 연령, 캐주얼 퍼즐·아케이드 유저 |
| 플레이 타임 | 1회 1~3분, 반복 도전형 (하이스코어 경쟁) |
| 코어 루프 | 관찰 → 발판 스케치 → 자동 점프 → 상승 / 추락 복귀 → 생존 / 재도전 |

## 4. 핵심 메커닉

- 캐릭터(먹방울이)는 **일정 주기마다 자동으로 점프**한다. 플레이어가 점프 자체를 조작할 수 없다.
- 플레이어는 화면에 **손가락으로 선을 그어 발판을 생성**한다.
- 발판의 **위치 · 각도 · 길이**가 다음 점프의 궤적(방향, 높이)을 결정한다.
- 그린 발판은 가파른 대각선에서도 캐릭터가 붙을 수 있다. 화면 양옆 `ScreenSideWall`은
  일반 접착 대상이 아니며 안쪽으로 튕긴다. 첫 충돌은 수평 7.5~9.0과 수직
  4.8~7.0의 강한 트램펄린 반동을 주고 0.18m 안쪽으로 즉시 분리한다. 0.45초 동안 수직 재반동을
  막고 접촉 유지 중 새 상승분을 잘라 급경사 먹선과 측벽 사이 반복 상승을 차단한다.
- 화면 좌우 0.95m는 화면에는 보이지 않는 먹선 금지 띠다. 스무딩 뒤 띠만
  잘라 가장 긴 내부 구간을 발판·먹 소모량으로 확정한다. 캐릭터와 겹친 선도
  보존하며, 처음 겹친 본체·분신과의 충돌만 실제 몸체가 0.08m 이상 분리될 때까지
  개별 유예한다. 렌더 그림 크기는 획 삭제 조건이 아니다. 빠른 입력의 해제 좌표와
  짧은 끝부분도 길이 검사 전에 포함한다. `돋는 먹발 결실`을 선택하면 본체와 모든 분신이 상승·하강 여부와
  관계없이 좌우 벽에 붙고, 일반 발판과 같은 자동 점프 주기로 화면 안쪽을 향해
  다시 뛴다. 붉은
  측면 위험 표시는 해태 습격의 느낌표·반투명 먹빛 예고에만 사용한다.
- 첫 자동 점프는 접지 중 충전하고, 이후 충전은 점프 정점부터 시작한다.
- 직접 그린 발판은 3.4초 동안 선명하게 유지된 뒤 기본 1.1초에 걸쳐 처음 그린
  쪽부터 마른다. 기본 최대 먹 용량 4.8m를 넘겨도 가장 먼저 그린 획의 시작점부터
  시각·`EdgeCollider2D`가 같은 진행도로 지워진다. 시간 만료와 총량 초과는 같은
  단조 소멸 경로를 써 서로 잘린 콜라이더를 되살리지 않는다. 먹 회복과 4개 발판
  제한은 사용하지 않으며, 비정상 입력 보호용 객체 상한만 둔다.
- 발판 붓결은 Main UI의 `LineSprite`를 우선 사용하고 없으면 절차적 갈필로 폴백한다.
- 발판을 잘못 그리거나 타이밍을 놓치면 캐릭터가 굴러 떨어진다 (기존 점프킹과 동일한 낙하 리스크).
- 전략의 핵심은 "내 점프 컨트롤"이 아니라 **"그림으로 만드는 길"**.

## 5. 캐릭터 — 먹방울이

번진 먹방울과 한지 붓자국을 모티프로 만든 팀의 독자 수묵 캐릭터.

**비주얼 스펙 (확정, `char_muk.svg` 참고 — 이미지 파일은 별도로 전달됨):**
- 몸통: 먹 번짐 텍스처의 불규칙한 스파이크형 검은 원 (붓 터치로 그린 잉크 블롯 실루엣)
- 눈: 크고 둥근 흰색 타원 + 검은 원형 동공. **눈썹 · 입 · 안광(하이라이트) 없음** — 표정은 눈 모양과 다리 포즈로만 표현
- 다리: 팔 없이 **다리만 존재**. 기본(점프 대기) 포즈는 무릎이 몸통 옆으로 뾰족하게
  튀어나온 산(山) 모양 실루엣 — 몸통 아래에서 위로 짧게 붙었다가 몸통 바깥쪽 높은
  지점(무릎)까지 솟은 뒤, 다시 아래 발까지 길게 떨어지는 꺾인 선. 발끝에는 작은 훅 모양 표시.
  → 웅크려서 점프를 준비하는 자세를 표현
- **그림자 없음** (땅에 닿는 그림자 연출은 추후 필요 시 게임 내 조명/이펙트로 별도 처리)
- 수집 오브젝트: 먹물방울 · 황금 붓 · 먹 방어막 · 먹분신의 4종 아이템을 사용한다.

**애니메이션 상태 (구현 완료):**
- 4×2 스프라이트시트 8프레임: idle · crouch · launch · rise · apex · fall · dive · land
- 사망 시 캐릭터가 먹 번짐으로 바뀌며, 해당 위치에 한지 먹 자국이 남는다.
- 기본 캐릭터 그림은 기존보다 약 15% 크게 표시한다. 피격 1·2단계는 더 낮은
  PPU를 사용하고 최대 체력 증가로 생기는 3단계 이상도 마지막 시트를 단계적으로
  확대해 먹이 번지듯 매 피격마다 커지지만 루트 Transform과 Collider는 유지한다.
- 본체는 기본 3칸이며 영구 성장 먹피 경로의 일반 세 노드로 최대 6칸까지 늘어난다.
  모든 분신은 기본 1/1 체력이고 마지막 `먹피 결실`을 열면 2/2가 된다. 각 개체는 머리 위에 자기 최대치만큼
  독립된 체력 칸을 가지며 상단 HUD에서 한 개체의 체력을 대표값처럼 보여 주지 않는다.
  판 시작에는 본체를 새 성장 스냅샷의 최대 체력으로, 새 분신은 먹피 결실 여부에 따라 1/1 또는 2/2로 초기화하며
  Play 중 재컴파일 뒤에도 성장 이벤트와 스냅샷을 다시 연결한다.
- 첫 12m 아이템은 먹분신으로 보장한다. 이후 분신 비중은 30m의 35%에서 250m 이후
  50%까지 높아진다. 기본은 획득 1회당 한 마리이며, 생존 최종 열매 `먹떼 결실`을
  해금하면 한 마리를 추가해 최대 두 마리를 함께 만든다. 전체 생존 상한은 24마리이고
  한 마리라도 살아 있으면 게임을 계속한다.

### 아이템 4종 확정 사양

| 아이템 | 효과 |
|---|---|
| 먹물방울 | 현재 먹떼 전체가 50m 상승. 상승 구간에는 장애물 피해를 받지 않음 |
| 황금 붓 | 8초 동안 총량 초과 정산을 보류하고 획을 유지. 종료 시 오래된 획부터 정산 |
| 먹 방어막 | 장애물 피해 또는 화면 하단 추락을 1회 막고 다시 상승 |
| 먹분신 | 기본 1마리, 최종 성장 `먹떼 결실` 선택 시 추가 1마리(획득 1회 최대 2마리, 생존 상한 24). 마지막 캐릭터가 죽을 때만 게임오버 |

- 아이콘 4종은 투명 1254×1254 PNG이며 실제 피사체 폭을 900px로 통일한다.
- 황금 붓이 활성화되면 카메라가 따라가는 생존 캐릭터 오른쪽 위에 작은 노란 시간 링 하나를 표시한다.
  프레임의 투명 여백을 제외한 몸통 가까이에 붙이며, 링 중심 높이는 머리와 체력 바 사이다.
  좌우 간격은 유지하고 체력 바와 겹칠 때만 세로 위치를 보정한다.
  본체·체력 표시를 피해 배치하고, 선두가 죽으면 다음 생존자를 따라간다. 화면 밖 개체의 링은 숨긴다.
  실제 남은 시간을 읽어 12시에서 시계 방향으로 비우고, 일시정지 중에는 멈춘다.
  시간이 연장되는 재획득에는 다시 채우고 만료·새 판에서는 숨긴다. 숫자와 반복 회전은 없다.
- 캐릭터와 같은 단순한 수묵 스타일, 제한된 검정·종이색·금색 팔레트, 굵기가 비슷한
  검정 외곽선을 유지한다.
- 먹분신은 넓고 납작한 먹 캐릭터 두 마리와 동일한 흰 타원 눈·검정 동공으로 표현한다.

### 영구 성장

- 메인 로비는 더 이상 첫 획으로 시작하지 않는다. `시작` 버튼을 누르면 씬 빌더가
  만든 영구 시작 먹선 위에서 플레이어와 먹선을 표시하고 물리를 풀어 1초 자동
  점프를 준비한다. 로비에서는 하단 먹방울과 시작 먹선을 모두 숨긴다. 로비 포인터는
  드로잉 입력으로 전달하지 않아 `시작`·`성장`·`옵션` 탭과 획 생성이 겹치지 않는다.
- 로비의 `성장`은 게임 시작 전에 여는 별도 영구 성장 화면이다. 정상 게임오버에서
  받은 단일 재화 `먹빛`으로 먹·먹물·붓·도약을 각각 8단계 키운다(v11, 총 32단계).
  먹은 체력, 먹물은 최대 용량이다. 표시 이름만 바꾸며 기존 body/ink ID·구매·저장은 유지한다.
  항목별 비용은 1·2·3·5·7·10·14·19, 총 비용은 244먹빛이다. 기존 단계의 비용은 유지한다.
  매 단계 체력 +1칸, 최대 먹 +12.5%, 먹 소모 -3%, 점프 높이 +1.25%를 적용한다.
  최대 본체 체력은 11칸, 먹 용량 2배, 먹 소모 76%, 점프 높이 110%이며 분신 체력은 1칸이다.
  v8 구매·지갑·거리·정산 이력은 보존하고, 구 39먹빛 상한을 넘어 쌓은 거리 보상 차액만
  한 번 지급한다. v7 이하 트리는 동결 검증 뒤 구매액을 환급하는 구 이관 경로만 남긴다.
  `초기화`는 구매액을 전액 환급하고 누적 거리·수령 이력을 보존한다.
  판 시작 시 구매한 모든 성장 효과를 스냅샷으로 고정해 진행 중에는 바뀌지 않는다.
- 먹빛은 정상 게임오버의 결과 고도를 누적한다. 첫 개 50m, 2~4개째 각 100m,
  5~12개째 각 150m, 13~24개째 각 250m, 25~40개째 각 350m, 이후 각 500m다.
  신규 200m + 300m = 총 5개이며 남은 m는 다음 판으로 이어진다. 마지막 문턱은
  112,150m(244개)다. 순번은 보유량이 아닌 기수령 수라 구매·초기화로 되돌아가지 않는다.
  v9 이하 및 v10의 균일 500m 규칙은 저장 검증·이관에서 동결한다. v11 이관은
  실제 거리의 새 보상과 기존 기수령분+미지급 거리를 모두 보존하고 차액만 지급한다.
  기존 보정 거리를 그대로 새 곡선에 대입하지 않고 `distanceRewardOffsetMeters`를 재계산한다.
  재로드·클라우드 교체에서 같은 이관을 중복 지급하지 않는다.
  성장 화면에는 현재 구간의 먹획 진행 막대, `먹빛 +1 · N / 구간m`,
  실제 누적 거리를 표시하며 반복 장식 모션은 넣지 않는다.
  디버그 판·중도 복귀·중복 run ID는
  거리를 더하지 않는다. 한 판에 여러 문턱을 넘으면 그 수만큼 일괄 지급한다.
- 영구 성장 저장은 정상 스냅샷을 별도 백업에도 함께 기록한다. 손상된 JSON이나
  지원하지 않는 미래 schema/balance 저장은 원본을 덮어쓰지 않고 읽기 전용으로
  잠근다. 이 상태에서는 시작을 성장 화면으로 돌려 `백업 복원` 또는 2회 확인이
  필요한 `새로 시작`을 선택하게 하며, 실패한 정산은 결과창에 복구 필요로 표시한다.
- `성장`은 로비 위에 겹치는 팝업이 아니라 불투명 한지 배경을 가진 전용 전체
  화면이다. `LobbyScreenNavigator`가 로비·성장 중 하나만 표시하고,
  `BrushTransitionView`는 기존 8개 붓 원화·배치·1.15초 진행을 유지하고 같은 원화로 6획을 더 이어 총 1.85초 동안 덮는다.
  별도 `BrushCoverGraphic` 합성 먹 메쉬는 사용하지 않는다. 끝부분의 기존 먹색 워시로 빈 픽셀을 부드럽게 마무리하고
  이전처럼 0.22초만 대기한다. 좁고 긴 화면·가로 화면의 입력 차단과 전체 화면 덮개는 유지한다.
  이어서 0.7초 드러남이 끝나야 입력을 다시 연다. 일반 게임 시작·성장 왕복·로비 복귀가 같은 전환을 쓴다.
  검정 덮개와 입력 차단은 Safe Area가 아닌 전체 Canvas에 stretch하고 붓결보다 위에서 완전히 가린다.
  암전·드러남 중 가려진 붓 14장은 렌더하지 않고 재사용한다. 씬 재로드는 대기를 중복하지 않는다.
  앱 비활성에서는 전환 시계를 멈추며 움직임 줄이기는 붓 이동 없이 짧은 먹색 페이드와 0.22초 대기를 사용한다.
  최초 실행 튜토리얼은 기존처럼 제작사 화면 뒤에서 즉시 준비한다. 옵션만 로비 모달로 유지한다.
- 최대 먹 용량·획당 먹 소모·회복·점프처럼 지속되는 수치는 `기본값 × 영구 성장 × 구간 배율`로
  합성하고, 본체 최대 체력과 먹분신 아이템 생성 수는 런 스냅샷을 읽는 각 소유 시스템에서
  적용한다. 본체 1회 부활·50m 상승·좌우 벽 자동점프·2단점프는 선택한 결실에서만 활성화하며 안전 발판은 사용하지 않는다.
  `RunGrowthController.ResetRun()`은 영구 성장 스냅샷과 판당 상태만
  초기화하며 `PermanentGrowthProfile` 저장은 건드리지 않는다.
- 생존은 `본체 체력 +1×3(결실: 분신 체력 +1) / 피격 무적 +0.04초×4(결실: 본체 1회 부활·50m 상승) / 피격 수평 밀림 -6%×3(결실: 먹분신 생성 최대 +1)`,
  도약은 `준비 -1.5%×3(결실: 점프 아이템 종료 방어막) / 점프 힘 +1%×4(결실: 좌우 벽 상시 자동점프) / 높이 +1.5625%×3(결실: 정점 직전 매 자동점프 40% 2단점프)`, 먹 운용은
  `최대 먹 용량 +37.5%×3(결실: 현재 용량 ×2) / 획당 먹 소모 -2%×4(결실: 황금 붓 획득 방어막) / 회복 속도 +10%×4`다. 최대 먹 용량은
  4.8m에서 일반 열매만으로 10.2m, 넓은 벼루 결실 적용 시 20.4m까지 성장한다. 판 한정 최대 먹 용량 확장 아이템은 사용하지 않으며,
  총량 증가는 영구 성장에서만 제공한다.
- `숨 고르기 결실`은 본체가 장애물 또는 화면 하단 추락으로 쓰러질 때 판당 한 번
  체력 1로 부활시키고 먹물 연출과 함께 현재 위치 기준 50m를 상승시킨다. 추락은 먼저
  화면 안쪽 안전선으로 복귀한다. 분신은 이 사용권을 소비하지 않으며 기본 체력 1,
  먹피 결실 뒤 체력 2이다. 아이템 방어막은 본체·분신이 각자 소유한다.
- 플레이 중 무작위 증강, 성장 두루마리, 성장 선택 카드와 도감은 사용하지 않는다.
  세부 노드와 저장 호환 규칙은 `docs/design/permanent-growth-trait-tree-v3.md`를 따른다.

## 6. AI 핵심 기능

### 6.1 로컬 수묵 스타일 변환

플레이어가 그린 발판 스케치(터치 스트로크)에 AI로 탐색·제작한 수묵 아트 방향을 고정한
로컬 갈필 스타일러가 즉시 붓질 질감을 적용한다. 그림 실력과 무관하게 화면 전체가
하나의 산수화처럼 통일되는 것이 핵심 경험이다.

파이프라인: **스케치 입력 → 로컬 갈필 스타일 → 발판 완성(인게임 콜라이더 반영)**

현재 런타임에는 원격 img2img 호출이나 API 키가 없다. AI 활용은 아트 방향·배경·아이콘·
VFX·사운드·코드 제작 과정과 `docs/ai-usage-log.md`에 기록된 사람의 검수 과정에 있다.

### 6.2 배경 구현과 확장 아이디어

- 구현: 0·250·500·750m에서 고요한 산길 → 바람 능선 → 먹비 계곡 → 검은 절벽의
  기본 수채화 배경 4종을 전환한다. 1000m부터는 먹빛 성문 → 월련 성해 → 천하수의
  동양 수채화 우주 3종을 250m마다 순환하고, 다음 순환은 좌우 반전해 반복감을 줄인다.
  모든 배경은 1초 교차 전환하며 중앙·하단을 밝은 한지 여백으로 유지한다.
- 밤 이스터에그: 메인 먹점프 로고의 실제 글씨 영역을 10번 탭한다. 해·달 직접 탭은 받지 않는다.
  매 탭마다 해·달이 원래 색·크기를 유지하며 곡선으로 조금씩 내려간다. 탭에 따른 붉은색 변화는 없다.
  열 번째도 현재 위치·속도에서 하강을 이어 간다. 5.8초 전환에서 기존 천체가 완전히 사라진 뒤
  반대편의 같은 크기 천체가 서서히 올라온다. 오른쪽 우하향·왼쪽 우상향은 같은 곡선의 좌우 반사이며,
  양쪽 도착점은 실제 배너 하단과 최고 기록 상단의 중간에서 대칭이다. 원화를 뒷배경과 258정점 앞산 Mesh로
  분리하고 달을 두 렌더 레이어 사이에 둔다. 원본 텍스처·UV·밤 색조를 유지하며 전체 알파로 사라지게 하지 않는다.
  로고는 메뉴 버튼과 같은 UI 이벤트 경로로 탭을 받는다. 움직임 줄이기는 이동 없이 짧은 색 전환만 한다.
  달은 해와 같은 크기의 둥근 실루엣을 유지하되, 가장자리에 완만한 작은 굴곡과 짧은 번짐만 남긴다.
  안쪽은 넓고 은은한 한지 얼룩으로 채우며, 빽빽한 점무늬 없이 명암 차이를 7% 이내로 제한한다.
  질감은 고정하며 해의 기존 원화와 탭·이동·색 변화는 유지한다.
  밤에도 로고 10번 탭으로 낮 복귀. 정교한 별도 밤 원화를 쓰지 않고 기존 7개 맵의 투박한 붓결을
  보존한 채 먹청색으로 어둡게 한다. 플레이 공간의 명암과 UI 색은 유지하고, 맵 전환·로비
  왕복 중에도 그 실행의 밤 상태를 공유한다. 새 실행은 낮. [구현·에셋](docs/design/lobby-night.md).
- 맵별 느린 배경: 산수화 자체는 고정하고 공통 카메라 배경의 `AmbientCloudView`가
  구름·바람결·비안개·연무·별가루·연꽃 안개·물결 7종을 맵에 맞춰 교체한다. 가로 이동은 최초 속도의 1.8배,
  10초에 화면 폭의 약 1.2~5.0%로 흐르며 맵별 방향·투명도·상하 흔들림은 유지한다.
  Low/Medium/High에서 고정 렌더러 2/3/4개. 로비·성장·플레이가 공유하며
  일시정지·앱 비활성·움직임 줄이기에서는 이동을 멈추고 맵 교체는 완료한다. [사양·원화](docs/design/map-atmospheres.md).
- 적응형 국악 사운드: 속도 · 위험도에 따라 국악기 기반 배경음을 AI가 실시간 변주
- 나만의 낙관(도장): 플레이 스타일을 분석해 엔딩에서 개인화된 도장 디자인을 AI가 생성

## 7. 기술 스택 & 구현 전략

- Firebase Analytics: 공식 Unity App/Analytics 13.16.0을 로컬 UPM tarball로 고정한다.
  iOS의 Apple ATT는 별도 동의다. 제작사 화면 종료·Main 준비·앱 활성/포커스가
  0.5초 유지된 뒤 미결정 상태에서만 시스템 창을 요청하고 이후 UMP/광고를 초기화한다.
  ATT 허용을 Firebase 분석 동의로 간주하지 않으며 거부해도 코어 게임은 유지한다.
  `MukJumpAnalytics`는 SDK와 분리된 타입 제한 이벤트/판별 합계, `FirebaseAnalyticsRuntime`은
  네이티브 전용 비차단 초기화·선택 동의를 처리한다. 기본 수집 거부, 에디터·토스는 네이티브 호출 금지,
  Development 기본 비활성이다. UUID·닉네임·원문 오류는 분석에 넣지 않는다.
  설정의 개인정보 링크는 네이티브에서 분석 선택+전문 링크 두루마리를 열며 dim으로 설정 복귀한다.
  Firebase `mukjump-3751d`의 iOS·Android 구성 파일을 `Assets/`에 연결했다. 기기 수신·DebugView는 아직 미검증이다.
  네이티브 빌드 전 Bundle 일치 검증을 강제하며, 현재 빌드·업로드는 사용자 요청으로 중단 상태다.
  iOS `FirebaseAnalytics/Core`와 IDFV 차단, 양 플랫폼 collection/consent 기본 거부를 빌드에 적용한다.
  게임오버·부활·최종 정산은 서로 구분하고 재화는 저장 성공 이후만 기록한다.
  자세한 이벤트·콘솔·개인정보 출시 게이트는 `docs/firebase-analytics.md` 참조.

- **엔진**: Unity 6 (6000.5.9f1), URP, Universal 2D 템플릿, iOS·Android·WebGL 빌드 타겟
- **소스 관리**: Unity Editor Version Control Mode = Git (Meta 파일 보이기 + 텍스트 직렬화 설정됨)
- **드로잉 처리**: 터치 스트로크 캡처 → 베지어 스무딩 → 물리 콜라이더 자동 생성
- **수묵 변환**: `SketchToInkService`와 `FallbackInkStyle`이 로컬에서 절차적 갈필
  텍스처를 적용한다. 네트워크 지연·실패 경로가 없고 API 키 없이 항상 동일하게 동작한다.

## 8. 아트 팔레트 (수묵 / 한지 톤)

| 이름 | 용도 | HEX |
|---|---|---|
| INK | 잉크 배경 / 다크 UI | `#1C1B1A` |
| INK2 | 잉크 패널 (약간 밝게) | `#26241F` |
| PAPER | 한지 배경 | `#EAE3D2` |
| PAPER2 | 한지 카드/구분 배경 | `#DFD6BE` |
| RED (낙관) | 강조, 도장, 포인트 | `#AE1C3C` |
| OBSTACLE_RED | 장애물 붉은 한지 본체 | `#C8645B` |
| GOLD | 서브 강조, 골드 잉크 | `#9C7A3C` |
| MAP_REST | 맵 쉼터 발판의 담녹색 | `#687956` |
| TEXT_DARK | 라이트 배경 위 본문 텍스트 | `#2B2620` |
| TEXT_MUTED | 라이트 배경 위 보조 텍스트 | `#514B42` |
| TEXT_LIGHT | 다크 배경 위 본문 텍스트 | `#F5F1E6` |

- 맵에 생성되는 쉼터 발판은 담녹색 수묵 질감과 얇은 한지 받침으로 구분한다.
  양 끝은 같은 두께를 유지하면서 마지막 약 4%만 불규칙한 먹섬유로 짧게 번져
  봉합되며, 수직 절단면이나 긴 붓꼬리를 만들지 않는다. 받침도 같은 마감 재질을 공유한다. 직접 그리는 먹선의
  갈필·테이퍼와 청회색 풍맥 표현, 실제 발판 충돌 위치는 유지한다.
- 전체 UI 서체는 `Assets/Resources/MukJump/Fonts/NanumBrushScript-Regular.ttf`
  (Nanum Brush Script, SIL OFL 1.1)를 사용한다. 결과창의 제목·이번/최고 고도 캡션과 수치·
  행동 버튼·신기록은 Bold로 강조하고 불투명 먹색을 쓴다(신기록 낙관은 한지색).
  글자 크기와 가운데 정렬은 유지하며 중복 그림자 없이 굵기로 가독성을 높인다.
- 일시정지의 짧은 제목은 80px Bold·불투명 먹색, 계속하기/로비로는 56px로 표시한다.
  첫 안내와 옵션의 튜토리얼은 제목 64px·본문 52px, 첫 안내 보조 문구는 44px를 사용하며
  자동 축소 없이 전체 문장의 줄바꿈·잘림을 검증한다. 한지·기존 서체와 동작은 유지한다.
- 성장 집중 화면의 최신 가독성 규격: 설명·효과 요약 56px, 구매 80px,
  보유 먹빛 68px, 선택 이름 56px, 로비·초기화는 아이콘만 사용한다. 효과는 `최대 먹 / +12.5%`처럼
  두 줄로 표시하고 비활성 구매 버튼도 글자는 밝은 한지색을 유지한다. 폰트 자동 축소 없이
  선택 영역·버튼 높이를 함께 늘린다. [가독성 규격](docs/design/growth-readability.md).
- 성장 화면 장식은 기존 갈필 링을 중앙 아이콘 뒤에 한 번, 먹·먹물·붓·도약에는 독립된
  한지 패널(각 240×386, 알파 0.94)을 사용한다. 네 패널은 기존 종이색·갈필 네모 디자인을
  공유하고 20px 간격으로 나란히 놓는다. 귀퉁이만 작게 둥글리며 별도 합성 윤곽선 없이,
  중앙 원형의 `pg_selected_ring` 원화와 같은 먹색·알파 0.26을 `GrowthRingFrameGraphic`으로
  각 네모 둘레에 펼친다. 두 겹 갈필·끊김은 원화를 공유하고 긴 직선과 작은 모서리를 유지한다.
  종이는 같은 질감의 9-slice로 모서리를 보존하고 테두리는 종이 안쪽으로 보정한다.
  글씨 뒤 중심 농도는 고르게 유지한다. 흰 구름 같은 큰 굴곡·붓 마스크의 구멍·별도 위아래 장식선·
  네 패널을 다시 묶는 큰 받침·세로 구분선은 없다.
  선택한 이름 뒤 붉은 번짐 하나(알파 0.20)·120×6 붓 밑줄·먹색 Bold 효과 글씨로 구분한다.
  장식은 입력을 받지 않으며 설명·효과·구매 글씨 크기와 로비 산수화 배경은 유지한다.
  붓은 게이지 옆 붓을 참고한 미니 원화 `growth_brush_mini_v1`을 큰 선택 아이콘과 카드에서 공유한다.
  두루마리 아이콘은 사용하지 않으며 게이지 자체의 붓 원화는 바꾸지 않는다.
- 성장 비용은 구매 버튼 위 별도 56px 줄에 `먹빛 N개 필요`로 표시한다. 버튼 문구는
  80px `성장하기`(영어 `Upgrade`)만 유지하며 부족할 때도 비용은 숨기지 않는다. 완료·선택 없음·복구 상태는
  비용행에서 안내하고, 실제 구매 가능 여부는 기존 재화·성장·복구·입력 잠금 조건을 유지한다.
  구매 버튼은 메인 시작 버튼의 `muk_start_button.png` 검정 먹 붓과 밝은 글씨·공통 눌림 반응을
  재사용한다. 붓·터치 영역을 820×200으로 넓히고 중앙 글씨를 유지하며 한지 테두리·붉은 얼룩은 넣지 않는다.
- 성장 헤더는 긴 꼬리 획·밑줄 없이 두 글자만 쓴 투명 붓글씨 원화
  `pg_title_growth_ko_v1.png`를 기존 제목 칸 가운데 표시한다. 영어에서는 즉시 기존 `Growth`
  제목으로 전환하고, 원화 누락 시에도 번역 텍스트를 유지한다. 반복 안내 문장은 생략한다.
  왼쪽 로비는 `<` 모양 뒤로가기, 오른쪽 초기화는 되돌림 화살표의 84×84 먹선 아이콘만 표시한다.
  두 아이콘은 기준 화면 중심에서 좌우 465px에 놓아 가장자리 여백을 각각 33px로 맞춘다.
  한지판·글자 없이 각각 220×120, 200×120 투명 터치 영역을 유지한다. 초기화 아이콘을 누르면
  `초기화하시겠습니까?` 한지 두루마리 팝업과 먹빛 환급 안내를 표시한다. 팝업의 `초기화`를
  눌러야 구매 성장만 초기화하고 사용한 먹빛을 돌려준다. `취소`·바깥 딤은 변경 없이 닫으며
  시간 제한이나 아이콘 두 번 누르기는 없다. 팝업이 닫힐 때까지 뒤쪽 입력을 막고 연속 확인의
  중복 환급을 방지한다. 보유 먹빛은 그 아래, 필요 먹빛은 하단 중앙에 둔다.
  메인 로비의 시작·성장·옵션 전용 PNG는 유지한다.
- 성장 진행 먹방울은 상단 먹빛과 같은 `pg_inklight_sumukhwa_v1` 붓방울 원화로
  64px, 중심 간격 80px로 표시한다. 소유는 원화 100%, 미소유는 같은 원화의 알파
  32%로 구분한다. 합성 물방울·종이색 속 그림은 없애고 각 항목의 8칸을 중앙에 맞춘다.
  하단 선택 영역은 받침 위아래로 25px, 진행 칸과 받침은 30px, 받침과 비용은 32px,
  비용·구매 버튼은 30px 띄운다.
  완료 시에는 기존 `다 자랐어요`로 교체한다.
- 상단 보유 먹빛은 `pg_inklight_sumukhwa_v1`의 갈필·먹 농담이 있는 단일 붓방울 원화로
  표시한다. 상단 84px·진행 칸 64px 크기와 흰 tint를 유지해 원화의 농담을 보존한다.

## 9. 에셋 인벤토리

이미지 파일은 이 문서와 별도로 터미널 Claude Code에게 직접 전달 예정. 목록만 참고:

- `char_muk.svg` — 먹방울이 캐릭터 벡터 원본 (수정 시 이 파일을 편집)
- `Assets/Art/Character/Player/character_muk_bangul_v3.png` — 캐릭터 래스터 (1024×1024, 투명 배경, 최신본)
- `Assets/Resources/MukJump/Obstacles/child_ink_dragon.png` — 초등학생의 굵은 먹선처럼
  단순화한 좌우 이동 동양 용 원본 (1536×514, 투명 배경)
- `Assets/Resources/MukJump/Obstacles/child_ink_dragon_4frame_v3.png` — 원본 인상을 유지한
  2×2 어린 용 루프 시트 (1536×1024, 셀 768×512, 좌상→우상→좌하→우하)
- `Assets/Resources/MukJump/Obstacles/child_ink_haetae_4frame_v2.png` — 검정·회색
  수채화 먹해태 수문장 상태 시트 (1254×1254, 2×2 셀 627×627,
  서기→웅크리기→돌진→착지)
- `bg_muk_landscape.svg` — 배경 벡터 원본 (세로 산수화, 1080×1920 기준)
- `background_ink_landscape.png` — 배경 래스터 (1080×1920)
- `Assets/Art/Background/Maps/map_00_quiet_mountain.png` — 0m 고요한 산길
- `Assets/Art/Background/Maps/map_01_wind_ridge.png` — 250m 바람 능선
- `Assets/Art/Background/Maps/map_02_ink_rain_valley.png` — 500m 먹비 계곡
- `Assets/Art/Background/Maps/map_03_black_cliff.png` — 750m 검은 절벽
- `Assets/Resources/MukJump/Background/Endless/map_04_ink_galaxy_gate.png` — 먹빛 성문
- `Assets/Resources/MukJump/Background/Endless/map_05_celestial_lotus.png` — 월련 성해
- `Assets/Resources/MukJump/Background/Endless/map_06_heavenly_ink_river.png` — 천하수

> 참고: 배경 PNG 하단 약 620px 구간(`y: 1300~1920`)은 의도적으로 비워둔 "한지 여백"이며,
> 이 영역이 실제 발판을 그리는 플레이 공간이다. Unity에 배경을 넣을 때 이 여백 기준으로
> 카메라 뷰포트/스크롤을 맞추면 된다.

## 10. Unity 구현 시 제안 폴더 구조

```
Assets/
  Art/
    Character/Player/      character_muk_bangul_v3.png, muk_spritesheet.png
    Character/Death/       사망 애니메이션 개별 프레임
    Character/Obstacles/   장애물 스프라이트
    Background/    bg_muk_landscape.svg, background_ink_landscape.png
  Scripts/
    Player/        AutoJump.cs, PlayerController.cs
    Drawing/       StrokeCapture.cs, BezierSmoother.cs, PlatformCollider.cs
    Items/         ItemSpawner.cs, ItemPickup.cs, 아이템 VFX
    Obstacles/     ObstacleSpawner.cs, FallingInkRockSpawner.cs
    AI/            SketchToInkService.cs (로컬 스타일), FallbackInkStyle.cs
    Core/          GameManager.cs, ScoreManager.cs, Pooling/
  Scenes/
    Main.unity
docs/
  ai-usage-log.md   AI 도구·프롬프트 활용 내역 (개발 중 계속 기록, 13절 참고)
  architecture.md   런타임 기능 경계와 생명주기 규칙
  design/lobby-ui-overhaul-2026-07-30.md 로비·성장·옵션 UI 영구 규격
  VFX/SKILL.md      Unity 2D URP Android VFX 설계·구현·모바일 최적화 기준
  VFX/PROJECT_IMPLEMENTATION.md 먹점프에 실제 적용한 VFX 예산·진단·실기 QA
```

## 11. 현재 저장소 상태 (2026-08-04 기준, 중요)

> 아래 먹나무·39노드 설명은 구 구현 기록이다. 현재 성장 UI·경제는 위의 v9
> 4항목×8단계 규격을 우선하며, 구 결실 패시브와 나무 지도는 현재 판에 적용하지 않는다.

- GitHub Public 레포 `Team-Swooord/muk-jump`, Unity 6000.5.9f1, URP 2D 프로젝트
- 명시적인 `시작` 버튼과 영구 시작 먹선으로 로비 → 플레이 → 게임오버 → 로비 →
  재도전의 전체 게임 루프 구현. 로비의 `성장`은 먹빛으로 생존·도약·먹 운용 각
  13개, 총 39개 1포인트 열매를 해금한다. 각 잔가지는 수치를 반복 강화한 뒤
  나무 꼭대기의 큰 결실로 끝나며, 도약·먹 운용에도 영구 결실 패시브가 있다.
  결과 두루마리는 이번 판 거리의 누적 게이지와 먹빛 획득량을 표시한다. 부활 선택 전에는 예상치로 구분한다.
- 플레이 중 우측 상단 일시정지판 구현. `Playing` 세션과 풀을 보존한 채 물리·입력·
  효과음을 멈추고, 계속하기 또는 붓 화면 전환 뒤 로비 복귀를 선택한다.
  로비 복귀는 한지 확인 팝업에서 `지금 종료하면 이번 판 기록이 저장되지 않습니다.`를 안내하고
  `종료` 확인 뒤에만 진행한다. 취소·바깥 dim은 일시정지를 유지하며, 이중 클릭·이전 화면의
  남은 콜백은 중복 이탈을 만들지 않는다. 확인창과 일시정지판은 말림 완료 뒤 전환한다.
  IMGUI 먹 게이지·금빛 입자·황금 붓 시간 링은 일시정지·전환 중 그리지 않아 검정 덮개 위에 남지 않는다.
- 1초 자동 점프, 대각선 드로잉 발판 접착·회전, 보이지 않는 화면 좌우 반동 벽과
  0.95m 먹선 금지 띠 구현
- Chaikin 스무딩, EdgeCollider2D, 기본 4.8m 최대 먹 용량과 4.5초 자연 소멸,
  FIFO 오래된 획 소멸 구현.
  하단 게이지는 현재 그리고 있는 획과 확정된 먹선의 실제 ledger 점유량을 반영한다.
  소멸 잔상이 화면에 남아 있어도 예산이 반환되면 다시 차며 실제 최대량을
  트랙 폭으로 표시한다. 용량은 트랙 길이만 바꾸며 붓획 두께와 붓 아이콘은 Safe Area
  폭의 12%·14%로 고정해 1179×2556 실기기에서도 작아지지 않는다.
  옅은 전체 실루엣은 최대 용량 안내로 유지하고, 실제 잔량은 불투명 채움의 폭으로
  표현한다. 붓이 붙은 오른쪽 끝을 고정해 소모 시 왼쪽부터 0.24초 동안 줄고,
  회복 시 0.75초 동안 오른쪽에서 왼쪽으로 서서히 다시 차오른다. 채움 UV도 같은 비율로
  잘라 부분 이미지가 눌려 보이지 않게 한다.
  게이지 중심선은 붓 이미지 높이 50%에 맞추고, 오른쪽 끝을 붓 폭 62% 지점까지
  넣는다. 붓을 마지막에 그려 빈 트랙·채움·금색 먹이 붓 몸통 중앙으로 들어가 보이게 한다.
  붓 크기와 Safe Area 좌우 여백은 유지한다.
- 첫 30m는 공격 장애물 없이 유지한다. 이후 이동 먹가시와 예고 후 낙하해
  발판을 파괴하는 낙묵석이 등장하고, 60m부터 5fps 4프레임으로 통통 움직이는
  좌우 이동 어린 동양 용이 합류한다. 먹해태는 320m부터 좌·우 한쪽 벽의 느낌표와
  반투명 붉은 먹빛이 먼저 1.1~1.2초 경고하고, 해태가 화면 위 바깥에서 실체화해
  선택된 벽을 따라 1.25~1.45초에 걸쳐 아래 화면 밖까지 직선으로 내려오는 먹해태가
  기존 이동 장애물 슬롯을 대체한다. 플레이어 위치를 조준하지 않고 먹선·일반 발판·
  풍맥에도 막히지 않는다.
  양쪽 모두 머리는 아래, 발은 해당 벽 쪽을 향한다. 왼쪽만 스프라이트 로컬 Y를 반전하며
  90도 루트 회전·충돌 판정·경로는 유지한다. 풀 반납 시 반전을 지워 반대쪽 재사용에 남기지 않는다.
  이동 장애물은 12~16m, 낙묵석은 저고도 7~10초·고고도 5.5~7.5초 간격으로
  완화한다. 큰 동물 비율은 용 20→12%·해태 8%이며 용·낙묵석·강풍의 위험 중첩은
  제한한다. 장애물과 아이템 가로 배치는 기기별 현재 카메라 폭을 사용한다.
  프레임 여백은 충돌 판정 크기에 영향을 주지 않는다.
- 아이템 4종 구현:
  - 먹물방울: 50m 상승과 상승 중 장애물 보호
  - 황금 붓: 8초간 오래된 획 퇴출 보류, 종료 뒤 총량 정산
  - 먹 방어막: 장애물 피해 또는 추락 1회 방지. 보유 중인 두 번째 방어막은
    소비하지 않고 필드에 남긴다.
  - 먹분신: 12m 첫 슬롯 보장, 이후 35→50% 가중 출현. 기본 1마리와 `먹떼 결실`
    보너스(0~1)를 한 번에 생성해 최대 24마리까지 누적하고 마지막 캐릭터 사망 시에만 게임오버
- 먹 번짐 사망 연출과 최대 20개까지 유지되는 한지 먹 자국 구현
- 화면 하단 추락은 해당 먹방울의 방어막을 우선 소모하고, 방어막이 없으면 체력
  1칸을 깎는다. 체력이 남으면 화면 안으로 옮겨 35m 회복 상승하며 마지막 체력이
  소진됐을 때만 사망한다. `숨 고르기 결실`을 선택한 본체만 장애물·추락 사망을
  판당 한 번 체력 1로 되돌린 뒤 먹물 연출과 함께 50m를 상승하며, 분신은 이 사용권을
  적용받지 않는다.
- 하단 추락선은 현재 카메라 크기를 매 물리 스텝 반영한다. 몸체 중심이 추락선 위에
  남아 있어도 화면 밖 발판에 착지하면 일반 착지·풍맥·자동 점프보다 먼저 같은 추락
  소모·복귀 경로를 실행해 하단 무한 점프를 막는다. 화면 안 발판의 정상 착지는 유지한다.
- 각 개체의 머리 위 체력은 자기 최대 체력 수만큼 독립된 칸으로 표시한다. 본체는
  3~6칸, 분신은 기본 1칸·먹피 결실 뒤 2칸이다. 실제 체력
  감소에는 캐시된 앞면 플래시→붉은 먹 번짐과 공용 풀 링·충돌음·약한 진동을 쓰며,
  캐릭터 루트 스케일과 Collider는 바꾸지 않는다.
- 본체 기본 최대 체력은 3이며 `S-A1 → S-A2 → S-A3` 먹피 일반 열매가 각각 +1을
  제공해 최대 6이 된다. 마지막 `S-KA`는 본체 수치 대신 분신 최대 체력을 1→2로 올린다. 기존 저장은 같은 node ID를
  유지한 채 새 효과로 해석한다.
- 점프·착지·연속 붓 드로잉·화면 전환·벽 충돌·아이템·캐릭터 사망·게임 종료 피드백과
  자체 생성 WAV·출처가 기록된 외부 MP3 효과음 구현
  - 장애물 유효 피격은 벽 충돌음 대신 0.16초 파열·급감쇠 저음·마찰음을 합성한 찰진 전용 타격음을 사용한다. 외부 게임 음원은 사용하지 않는다.
    생존·치명타 모두 재생하고 무적 접촉은 제외한다. 방어막은 기존 파괴음을 유지하며,
    70ms 안의 먹떼 동시 피격은 하나로 합친다. 효과음 설정을 따르고 생성 클립을 재사용한다.
  - 사망음은 짧은 고음과 먹방울 파열 질감을 합친 자체 생성 0.17초 `찍·팍` WAV이며,
    누락 시에도 같은 파형을 합성한다. 종료음은 사망음이 끝난 뒤 0.04초 간격을 둔다.
    종료음은 `GameOverSound`가 합성하는 0.85초 낮은 북 한 타·먹 마찰 잔향을 쓴다. 딩동처럼 들리는 현의 음정 진행과 종소리는 쓰지 않는다.
    최초 한 번 생성해 재사용하며 효과음 음량·음소거와 기존 재생 타이밍은 유지한다.
  (`Assets/Resources/MukJump/Audio/SFX`)
- Suno 생성곡 `Inkdrop Ascent` 반복 BGM과 로비·플레이·게임오버 상태별 음량 페이드 구현
- 최고 고도만 점수로 사용하며 250m마다 먹비·낙묵석 빈도 변화 구간 순환
- 작은 흰색 행동 버튼·설정 토글·먹빛 안내창·상단 HUD는 성장 카드의 불규칙한 흰 한지와
  `pg_selected_ring`의 겹친 갈필 테두리를 공유한다. `HanjiCardFrame`은 종횡비에 맞춰 테두리를 배치한다.
  메인 검정 붓 버튼·두루마리 본체·공식 Apple 컨트롤은 유지한다.
- 상단 HUD는 가운데 `고도 4m`를 64px로 크게, 오른쪽은 28px `최고` 아래 42px 수치로
  구분한다. 평상시 이름 문구는 숨기고 66px 바람 원화와 굵은 먹 화살표로 풍향을 표시한다.
  강풍·상승기류만 짧은 경고를 표시하며 일시정지는 두 갈필 먹획 아이콘을 쓴다.
  10,000m부터 km 단위로 압축한다. 광고와 10px 간격·기존 HUD 높이·투명 터치 영역은 유지한다.
  한지 띠는 완만하고 불규칙한 윤곽과 약 2px의 짧은 번짐으로 마감한다. 글자·낙관·배치는 고정하고
  반복 펄럭임은 추가하지 않는다. 좌우 끝은 충분한 높이를 유지해 붓꼬리로 가늘어지지 않는다.
  플레이 중 도전 시작 시점의 최고 고도를 처음 넘으면 최고 기록 숫자가 붉게 바뀌고
  작은 `신` 낙관이 찍혀 해당 플레이가 끝날 때까지 유지된다.
- 상단 풍향은 66×66 둥근 수묵 아이콘과 80×64 먹 화살표로 표시한다.
  경고·상승기류에는 중복 바람 아이콘을 숨기고 44×44 화살표와 짧은 문구를 같은 중심선의 한 줄로 표시한다.
  긴 별도 기류 예고는 띄우지 않는다. 상승 바람선은 최대 4개, 알파 0.12 이하의 짧은 청회색 획으로
  HUD 아래에서만 천천히 올라가며 양 끝에서 사라진다. 물리 효과·발생 간격은 유지한다.
- 모든 맵에서 먹선·한지 스타일 상단 풍향 HUD 방향으로 약한 횡풍이 불며, 바람 고개에서는
  조금 강해진다. 풍향은 28~45초 동안 유지한 뒤 천천히 바뀌며, 강풍 예고·상승기류·회복
  상태에서는 방향 유지 타이머를 멈춘다.
  첫 180~260m 이후 220~340m 간격으로 상승기류가 예고되며 5.5초 지속한다.
  상승기류는 상승 중 중력을 65%로 줄여 점프를 돕고 낙하를 강제로 뒤집지 않는다.
  수직 기류의 25%는 하강기류로, 상승 중 중력을 135%로 적용해 점프 높이를 낮춘다. 수직 기류 중에는 횡풍을 중복 적용하지 않는다. 좌우 횡풍은 접지하지 않은 공중 개체만 서서히 민다.
  별개의 광풍은 1,100~1,600m 간격, 1.8초 예고, 3.5초 지속이다. 강한 난류와 최대 약 90도 몸 흔들림을 적용하며 가장자리에서는 안쪽으로 유도한다.
  광풍은 발판 접착·먹물방울 특수 상승을 제외한다. 종료 후 0.8초 동안 중력을 복구하며 광풍 중 낙하 비기는 소모하지 않는다.
- 안전 먹 발판과 2.4초 휴식·피격 면역은 제거했다. 일반 발판은 먹색, 풍맥 발판은
  검정 붓 외곽의 청회색으로 구분하고 바람 문양을 유지한다. 풍맥 발판은 아래에서 위로
  통과할 수 있고 빠른 풍맥 상승은 위에 착지했을 때만 발동한다.
- 맵 단계는 0m 산길·250m 바람 능선·500m 먹비 계곡·750m 검은 절벽 이후,
  1000m부터 먹빛 성문·월련 성해·천하수의 밝은 한지 우주를 250m마다 무한 순환한다.
  같은 3종의 다음 순환은 좌우 반전하며 모든 전환은 1초 교차 페이드한다.
- 이동 먹가시와 낙묵석은 플레이어보다 위에 표시하고 본체 명암·알파를 보존한 채
  `#C8645B` 붉은 한지색으로 치환한다. 기존 받침·외곽선·과도한 크기 펄스는 사용하지
  않으며 낙묵석 예고의 밝기·가장자리 안전 여백은 유지한다.
- 아이템 화면 진입 예고, 강한 점프 카메라 줌·흔들림, 유효 피격 순간 정지,
  착지·방어막·사망 모바일 진동을 적용
- 대량 먹분신에서 카메라는 가장 높은 생존 먹방울을 따라 선두를 놓치지 않는다. 위험물·
  맵 진행만 먹떼의 하위 중앙값을 따라 돌출 상승이 난도를 조기 해금하지 않게 한다. 새 분신은
  획득 캐릭터의 보이는 외곽 바로 옆에 같은 높이로 배치하고, 화면 가운데에서는 좌우를
  번갈아 쓰며 가장자리에서는 안쪽을 우선한다. 눈·다리 없는 먹
  몸통이 0.12초 맺힌 뒤 완성 프레임이 0.18초 동안 뿅 나타나며, 분신마다 고정
  보조 렌더러 하나만 재사용한다. 새 분신은 기본 1/1, 먹피 결실 적용 시 2/2 체력이다.
  생성 순간 좌우 바깥쪽과 위로 인덱스별 탄성을 받아 일렬로 굳지 않으며, 먹물방울·부활 같은 특수 상승 속도는 더하지 않는다. 분신용 방어막 입자는 4개·파편은 6개로 줄이고
  0.14초 안의 동시 사망 피드백은 한 번만 낸다.
- 카메라는 가장 높은 생존 먹방울이 화면 높이 55% 부근에 오면 평소에는 위로만 따라간다.
  높은 본체·분신이 죽은 경우에만 남은 먹떼를 화면 46%에 두도록 0.5초간 한 번
  재구도한 뒤 다시 상승 전용 추적을 사용한다. 변경 전 34% 선행 구도와 과보정된 75% 데드존의 중간 구도라 위쪽 진행
  공간을 확보하면서도, 같은 높이의 반복 점프는 카메라를 누적 상승시키지 않는다.
  먹물방울 50m 급상승에는 뷰포트 90% 상단 안전선이 별도로 개입한다.
- 캐릭터와 겹친 획도 온전히 생성하고, 생성 순간 겹친 개체만 분리될 때까지 충돌을 유예한다.
  다른 개체의 착지·먹 비용·수명은 유지하며, 양쪽 먹벽 띠만 기존처럼 제외한다.
- 로비·HUD·디버그·알림·결과창·일시정지판은 Nanum Brush Script로 통일한다.
  작은 화면에서 고도·최고 기록·풍향·버튼·결과 안내가 읽히도록 글자 크기·굵기·
  먹색 명암을 높였으며, 결과창은 기존 젖은 한지 원화를 곡선 메시로 표시한다.
  사각 배경·테두리·큰 양끝 장식 대신 작은 목축과 말린 한지 롤 원화를 사용한다.
  위 축은 고정하고 아래 롤이 0.28초 동안 내려오며 원래 UV를 위에서부터 드러낸다.
  펼쳐진 뒤에는 종이 가장자리·아래 롤만 느리게 펄럭이고 글자·버튼·터치 영역은 고정한다.
  숨겨진 버튼은 펼침 완료 전 입력을 받지 않으며 움직임 줄이기는 정지된 열린 한지,
  앱 비활성·팝업 닫힘에서는 반복 모션을 중지한다.
  결과 제목·이번 고도·최고 고도는 모두 한 중심선에 가운데 정렬한다.
  최고 고도도 캡션 아래 수치를 배치하고, 먹빛 누적 게이지 → 부활·메인 버튼으로 이어진다.
  결과 게이지는 정산 전 거리에서 이번 판 고도를 더하며 1.1~1.76초 동안 숫자와 먹획이 함께 찬다.
  보상 문턱은 해당 구간 분모로 짧게 가득 찬 뒤 다음 구간 분모·남은 m로 이어지고 먹빛 획득 수를 한 번 강조한다.
  많은 문턱도 강조는 최대 3회로 합친다. 부활 선택 중에는 읽기 전용 예상치이며 실제 지갑은 변경하지 않는다.
  저장 재시도/미리보기 확정은 같은 게이지를 되감지 않으며, 메인·부활·닫기는 연출을 즉시 마친다.
  움직임 줄이기는 최종 값만 표시한다. 같은 고정 UI를 모든 품질 등급에서 재사용하며 반복 파티클은 없다.
  별도 정산 장부·보유량·부활 설명 블록은 표시하지 않는다.
  부활 버튼에는 `광고 시청하고 부활하기`만 표시하며 실제 50m 상승·방어막 보상은 유지한다.
  실제 저장 문제에만 짧은 안내와 복구·포기 확인 버튼을 제공하며 정산 로직은 유지한다.
- 로비 메뉴는 `시작 → 성장 → 옵션` 한 열로 배치한다. 세 버튼은 사용자가
  조정한 최고 기록 칸과 같은 `610.273×130.157` 배경, 배경 X `+89`, 텍스트
  `-87/-5`, `400×80`, Bold 46px 보정을 공통으로 사용한다. 같은 PNG 계열 버튼을
  추가할 때도 이 공통 규칙을 우선한다. 내부 보정은 유지한 채 메뉴 그룹은 화면
  중앙 X `0.50`에 두며 최고 기록도 같은 중앙 레일을 사용한다. 현재 선택된 메뉴
  배경은 100% 먹 농도, 나머지 배경은 78%로 표시하며 로비 기본 선택은 `시작`이다.
  세 버튼의 글자는 선택 상태와 무관하게 모두 100% 농도를 유지한다.
  구버전 Play 백업의 로비 왕복 이동은
  비활성화하며, 하단 먹방울과 시작 먹선은 `시작`을 누르기 전까지 보이지 않는다.
- 로비의 `시작·성장·옵션` 세 버튼은 기존 전용 PNG를 유지한다. 그 밖의
  흰색 행동 버튼·옵션·작은 안내창은 성장 카드의 종이 질감과 갈필 프레임을 공유한다.
  `InkUiStyle.ActionButtonSprite`는 공통 종이의 9-slice 변형이며 `HanjiCardFrame`이 테두리를 배치한다.
  주요 행동만 따뜻한 종이 tint와 안쪽의 옅은 낙관색·먹 번짐으로 구분한다.
  행동 버튼 글자는 Bold 40px 이상, 화면별 기존 가독성 크기·터치 영역은 유지한다.
  공식 Apple 로그인 컨트롤과 맨 아래 약관 텍스트 링크는 각자의 형식을 유지한다.
- 로비 `성장`은 전용 전체 화면에서 `생존·도약·먹 운용` 세 계보를 큰 수묵
  먹나무 한 그루의 여러 주가지·곁가지로 보여 준다. 나무 지도만 화면보다 크게
  만들고 양축 `ScrollRect`로 손가락·마우스 드래그 탐색한다. 최초 화면은 뿌리와
  각 계보의 첫 열매를 보여 주고, 39개 1포인트 노드는 여러 갈래를 따라 위로
  펼쳐진다. 지도 뷰포트는 Safe Area 바깥까지 기준 해상도 `1080×1920` 전체를
  패딩 없이 사용하고, 고정 로비·재화 HUD만 Safe Area 안에 둔다.
  먹나무 캔버스는 9:16에서 기본 66% 축척으로 보여 뿌리와 세 계보 시작점을 첫
  화면에 담고, 19.5:9·20:9에서는 실제 논리 폭에 비례해 나무만 더 축소한다.
  생존은 왼쪽, 먹 운용은 가운데, 도약은 오른쪽 큰 주가지로 분리하고 각 계보도
  실제 선행 조건에 따라 갈라진다. 각 잔가지 마지막 결실은 일반 열매보다 조금 크고
  금빛 후광을 두르며 마지막 일반 열매보다 약 310px 위에 배치한다. 생존 피격 여유 줄은 4단계 뒤
  1회 방어막, 피격 안정 줄은 3단계 뒤 먹분신 +1 최종 패시브로 끝난다. 도약 계보는 먹 운용의
  오른쪽 열매와 터치 영역이 겹치지 않도록 가지·표찰 전체를 오른쪽으로 띄운다.
  상단 제목·설명은 없애고, 상세창과 같은 94% 진회색 먹 패널 하나에
  `먹빛 아이콘 + 숫자`·누적 거리·세 계보의 실제 성장 요약을 함께 고정한다. 누적
  거리 막대는 길이 520px로 두고 현재 구간 진행률을 `0 / 100%` 형식으로 내부에
  표시한다. 같은 패널의 `노드 초기화`는 2회 확인 뒤 구매 노드만 환급한다.
  기본 글자는 밝은 한지색으로 두고 계보별 강조색만 유지하며, 로비 버튼은
  나무를 가리지 않도록 Safe Area 왼쪽 아래에 고정한다.
  모든 봉오리는 100% 불투명한 먹 원형 받침 위에 올려 한지·가지와 겹쳐도
  실루엣이 읽혀야 한다. 계보 표찰도 불투명 먹바탕을 사용하되 보조 글자는
  Normal 굵기로 둔다. 둥근 열매 노드를 고르면 화면 중앙의 짧은 한지 팝업에
  `아이콘 + 이름`, 효과 수치 한 줄, 설명, 비용과 행동 버튼만 표시한다.
  상태·적용 방식·`핵심 효과` 접두 문구처럼 같은 뜻을 반복하는 정보행은 만들지 않는다.
  아이콘·이름은 불규칙한 먹 테두리의 실제 가시 영역보다 안쪽에 두고,
  설명은 Normal 굵기이며 합성 외곽선을 사용하지 않는다. 별도 닫기 버튼은 두지 않고
  카드 밖 DIM을 눌러 돌아간다. 강화·적용 상태 버튼은 정보판과 충분히 떨어진 카드
  하단에 두며 모든 요소는 카드 경계 안에 둔다. 잠긴 노드 선택이
  구매로 이어지면 안 된다. 지도에는 노드명·단계 글자를 반복하지 않는다.
  네 변에 투명 여백이 있는
  `pg_tree_background_v3` 한 그루를 비율 유지로 모든 열매 뒤에 먼저 깔고,
  큰 나무는 약 42% 불투명도로 낮춰 열매·가지·고정 HUD보다 먼저 읽히지 않게 한다.
  별도 줄기와 장식 가지는 겹치지 않는다. `pg_branch_piece_01..06`은 조각별
  투명 여백을 보정해 실제 부모 간선 양끝을 열매 안쪽까지 이으며, 후속 특성 확장
  부분만 회전·확대해 보강한다. 조각이 없으면 `pg_branch`로
  폴백한다. 나무와 가지는 해금 여부와 무관하게 고정 먹색을 유지한다.
  성공 구매에만 먹 번짐·금빛·성장 낙관 피드백을 낸다. 성장 노드 상세창은 이름,
  계보, 효과, 설명, 비용, 행동 버튼까지 모두 Normal과 고정 글자 크기를 사용하고
  합성 외곽선도 끈다. 자동 축소와 Overflow는 사용하지 않는다.
  `설정`은 제목·버전·UUID 아래 배경음/효과음/진동 세 토글을 가운데 정렬하고,
  한국어/고객센터/튜토리얼/닉네임 두 열, 네이티브 계정 연동, 정책, 닫기 순서다.
  닉네임 버튼은 기존 수묵 붓 원화를 사용하고 계정 연동은 바로 아래 중앙에 둔다.
  토스는 플랫폼 닉네임 정책을 유지하며 자체 닉네임 변경 메뉴를 생성하지 않는다.
  버전과 구분선 사이에는 작은 `UUID` 한 줄을 표시한다. 네이티브는 현재 인증된 뒤끝 UID,
  토스는 현재 검증된 게임 사용자 hash를 사용하며 연결 전에는 `UUID —`로 표시한다.
  배경 없는 120 높이 터치 영역에서 전체 ID를 복사하고 짧은 완료 표시만 낸다.
  긴 ID는 화면에서만 줄이며 계정 전환·검증 해제 시 이전 ID를 지운다. 로컬 대체 ID는 만들지 않는다.
  아홉 신규 수묵 아이콘은 공통 한지 버튼 위에서 원화 색·알파를 보존한다.
  언어 메뉴에서 한국어/English를 선택하면 로비·HUD·성장·결과·튜토리얼·설정·계정·순위
  문구가 즉시 전환된다. 한국어가 기본이며 언어는 기기별로 저장하고 클라우드 성장/계정
  기록에는 섞지 않는다. 닉네임·문의 코드와 제작사 CYSBand 로고는 번역하지 않는다.
  닉네임 변경·Apple 첫 닉네임 설정은 같은 입력 팝업을 사용한다. 키보드의 좌상단 기준
  영역을 Safe Area의 좌하단 기준으로 변환해 키보드 위에 표시하며, 닫으면 원래 배치로 돌아간다.
  새 닉네임은 2~10자이고 입력 UI·로컬 저장·뒤끝 제출 전에 공통 비속어/성적 표현 필터를 적용한다.
  필터는 한·영 단어와 자모 분리·기호·숫자 치환을 로컬에서 검사하는 규칙 기반이며 완전한 차단이나
  변조 클라이언트의 서버 제출 방지를 보장하지 않는다. 자동 게스트는 `guest`+5자리 숫자를 사용하고
  중복 시 새 이름으로 재시도한다. 구 게스트 사용자 지정 이름도 자동 이름으로 정리한다.
  Apple 연동 계정의 사용자 지정 이름은 보존하며 게스트의 직접 닉네임 변경은 허용하지 않는다.
  설정 UID는 `Backend.UID`의 숫자 값 전체를 표시·복사한다. 서버 UID가 없으면 `UID —`로 표시하며
  로컬 ID를 대체 표시하지 않는다. UID 캐시는 계정별로 분리하고 저장 소유권은 기존 UserInDate를 유지한다.
  설정에서도 UID가 없으면 사용자 정보 조회를 시작하며 최대 5초 간격으로 재시도한다.
  설정과 계정 연동창은 같은 서버 UID를 사용하고, 로컬 식별자를 서버 UID처럼 표시하지 않는다.
  언어 변경창에는 한국어·English만 두고 `옵션으로` 버튼은 만들지 않는다.
  바깥 dim은 언어를 바꾸지 않고 설정으로 한 단계 돌아가며, 언어 선택 시 즉시 적용 후 설정으로 복귀한다.
  언어창 진입·복귀도 공통 접기→펼치기를 사용하고 전환 중 중복 입력을 막는다.
  게임 로고는 한국어 `먹점프`와 영문 수묵 원화 `MUK JUMP`를 공유 바인딩으로 교체한다.
  영문은 J 위쪽 획을 M 위를 지나도록 길게 빼 끝을 가늘게 마무리하고 P 옆·가운데·밑 장식 획은 없다.
  승인 원화는 실제 Resources의 `muk_logo_en.png`에 반영하며 체크무늬 없는 투명 PNG를 쓴다.
  로비가 숨겨진 상태의 언어 변경에도 즉시 반영하며, 두 원화의 유효
  먹획 폭·중심은 UV로 맞춰 기존 배치를 유지한다.
  동적 UI 문구는 `InkLocalizedText.SetSource`로 원문을 전달해 비활성 페이지도 즉시
  번역한다. 씬의 라벨은 빌더에서 바인딩하고 첫 Canvas 렌더 전에도 언어를 확인한다.
  광고 테스트 배너·보상/취소 안내도 번역하지만 실제 광고 소재와 네이티브 SDK 창은
  광고사/플랫폼이 언어를 결정한다. 번역 테스트는 강제 새로고침 전의 문구를 검사한다.
  고객센터는 메일 작성으로 연결하고 토스에서는 공식 OpenURL 브리지를 쓴다.
  소리·화면은 메인 메뉴에서 제거한다. 광고 개인정보 버튼은 메인/상세 설정 모두
  생성하지 않는다. UMP 초기 동의·광고 요청 가능 여부 처리는 변경하지 않았다.
  UMP가 재선택 진입점을 요구하는 네이티브 환경은 대체 경로를 마련하기 전 출시 검수 미완료다.
  계정 창은 제공자 연동 상태/필요한 행동만 표시한다. 연동 완료 시 로그인 버튼을 숨기되
  인증 만료 시 원래 제공자의 재로그인 버튼은 보존한다. 문의 코드는 복사 버튼으로 제공한다.
  순위 진입은 메인의 월계관·최고 기록 통합 붓버튼으로 통일한다. 검정 붓패널 안에
  72×72 월계관과 흰 기록을 함께 중앙 정렬하고 패널 전체가 같은 눌림 반응과 터치 영역을 쓴다.
  긴 기록은 32~46 크기로 맞추며 바깥 분리형 아이콘과 설정의 순위 버튼은 사용하지 않는다.
  설정을 거치지 않고 순위만 한 번 펼치며 dim은 역순으로 닫아 로비에 돌아간다.
  전환 중 중복 입력은 무시하고 순위 조회 갱신만으로 두루마리를 다시 펼치지 않는다.
  순번/닉네임/고도 세 열이며 실제 뒤끝 전체 기간 TOP 10을 조회한다.
  에디터를 포함해 샘플 순위를 덮어씌우지 않는다. 순위창의 작은 연결·빈 목록·오류 문구는 표시하지 않으며 내부 상태와 재시도는 유지한다.
  구 저장의 20자 닉네임도 모델에서 보존하고 뷰가 실제 폰트 폭에 맞춰 생략한다. 새 닉네임은 최대 10자다.
  게임 정보 저장과 리더보드 쓰기는 한 번씩 순서대로 진행한다. 정상 판 정산 이력이 있어야
  순위에 제출하며 로그인·연동만 한 새 계정의 0m는 등록하지 않는다. 실제 완료한 0m 판은 제출할 수 있다.
  게스트도 자동 인증 후 순위를 조회하며, 인증 대기 중 연 순위창은 연결 완료 후 자동으로 조회를 이어간다.
  닉네임 변경은 검증된 저장 이후 순위를 갱신하고 이미 조회한 목록도 다시 읽는다.
  이름이 없는 기록은 익명 문구로 대체하며 내부 계정 ID를 노출하지 않는다.
  Apple Game Center는 별도 탭과 GameKit 브리지를 사용한다. 콘솔 확인 전
  `appleGameCenterLeaderboardId`는 비어 있고 실제 Apple 조회·제출은 비활성이다.
  다른 플랫폼 기록을 동일인으로 추측해 병합하지 않는다.
  [사양·원화·검증 범위](docs/design/settings-ink-icons.md).
  이용약관/개인정보처리방침은 종이 안 맨 밑 좌우 끝의 32px 밑줄 텍스트 링크다.
  종이 버튼 배경은 없애고 340×120 투명 터치 영역과 실제 정책 URL은 유지한다.
  닫기는 두루마리 밖 아래 중앙의 96×96 한지 ×이며 터치 영역은 120×120이다.
  설정의 튜토리얼은 같은 배율·상단 축을 유지하고 진입·복귀 때만 접힌 뒤 다시 펼친다.
  이전/다음은 한지판 없는 `<`·`>` 아이콘이며 첫 이전·마지막 다음은 비활성화한다.
  `옵션으로`·완료 버튼은 없고 바깥 dim으로 설정에 돌아간다. 설명 페이지만 넘길 때에는
  두루마리를 다시 펼치지 않는다. 첫 실행 안내의 완료 저장·시작하기는 별도로 유지한다.
  튜토리얼·계정·언어의 dim은 설정 복귀, 메인에서 연 순위·설정·나머지 하위 페이지의 dim은 Close 경로를 사용한다. 종이 안 빈 공간,
  드래그·오른쪽 클릭은 제외하고 동기화 중 닫힘 제한 및 역순 말림을 유지한다.
  설정·튜토리얼은 820×1440 예약 공간으로 같이 축소하며 실제 위 축 높이를 맞춘다.
  다른 하위 페이지는 기존 크기/중앙 정렬을 유지한다.
  일반 설정의 게스트 소개와 반복 조작 설명은 생략한다. 실제 오류·삭제·기록 교체 경고만 남긴다.
  앱인토스 WebGL은 Apple/Google·계정 삭제·네이티브 UMP를 생성하지 않는다.
  메인 월계관 아이콘에서 설정창을 남기지 않고 공식 토스 게임센터를 바로 연다. SDK의 임의 사용자 목록
  조회 API가 없어 뒤끝/Apple 행과 합친 가짜 순위는 만들지 않는다.
  사용자 확인 실패 때만 기록 확인/재시도 페이지를 쓴다.
  토스 게임 hash는 현재 로컬 소유자 확인용이며 성장 클라우드 동기화는 아직 없다.
  기기 간 성장은 사용자별 자체 서버 저장으로 구현하기로 결정했다.
  [설계·현재 상태·완료 게이트](docs/design/apps-in-toss-identity-sync.md).
  움직임 줄이기 버튼은 메인·상세 설정에서 모두 제거한다. UI 두루마리의 무한 펄럭임과
  롤 흔들림은 사용하지 않으며 열기·역순 닫기·버튼 눌림만 유지한다. 배경/게임 효과는 유지한다.
  세 토글은 별도 사각 테두리 없이 공통 한지 원화 한 장을 9-slice로 표시한다.
  세 토글은 220×256 터치 영역과 80px 아이콘을 사용한다. 종이 위아래 24px,
  아이콘–이름 18px, 이름–상태 10px를 확보하고 켜짐/꺼짐 글자와 먹 농도로 상태를 구분한다.
  설정 제목은 72px·중앙 y516으로 위 롤과 떨어뜨린다. 버전·UUID·구분선도 함께 옮기되
  UUID 터치는 토글과 겹치지 않는다. 토글과 2열 메뉴는 50px, 메뉴 행은 16px,
  계정 앞은 36px를 띄우고 계정 아래에는 오류 안내와 약관 글씨의 별도 공간을 유지한다.
  설정·튜토리얼은 같은 상단 축과 배율을 사용하고 계정 연동 종이는 안전영역 정중앙에 둔다.
  계정 행동 행이 달라져도 중심은 고정하며, 위 토스트와 아래 외부 닫기 버튼까지 잘리지 않도록 필요할 때만 축소한다.
  계정 상태와 Apple 버튼 사이에는 빈 안내 행을 두지 않고, UID가 없으면 해당 자리도 없앤다.
  일반 연결 오류·복사 안내는 종이 위 먹색 토스트로 약 4초간 표시한 뒤 페이드아웃한다.
  같은 상태 갱신으로 반복 표시하지 않으며, 펼침/인증 복귀 전에는 표시 시간을 소진하지 않는다.
  계정 화면을 떠나면 토스트도 지운다. 기록 선택·동기화 복구 안내는 기존 차단 팝업에 유지한다.
  계정 진입과 바깥 dim/외부 닫기의 설정 복귀는 접기→펼치기로
  전환하며 중복 입력과 네이티브 Apple 버튼을 잠근다. 기록 선택·복구 중에는 복귀하지 않는다.
  상세 음량 행·켜짐 버튼·햅틱/움직임·계정 보조 버튼도 같은 한지 재질이며
  단색 사각 박스는 쓰지 않는다. 슬라이더와 구분선은 가는 붓결, 손잡이는 한지 속의 먹고리다.
  음량 채움은 원화 UV를 잘라 표시하고 실제 값·복구 음량은 유지한다. 공식 Apple 버튼은 제외한다.
  상세 `소리·화면` 구현은 보존하되 메인 진입 버튼은 제공하지 않는다. 빠른 토글은 마지막 음량을 복구한다.
  로컬 UID·미지원 언어·알림·쿠폰 목업은 추가하지 않는다. 튜토리얼 다시 보기는
  `GameplayTutorialCatalog` 및 `GameplayTutorialLayout`의 3쪽 설명서이며 첫 실행 스포트라이트와 구분한다.
- 실행 시 기존 CYSBand 제작사 로고에서 메인으로 연결한다. 신규 설치는 게임 안내, 완료자는 로비다.
  명시적인 계정 삭제가 서버·기기에서 모두 완료되면 새 게스트로 전환하고 계정 창을 닫은 뒤
  첫 안내를 다시 시작한다. 로그아웃·일반 재실행·클라우드 설정 복원은 완료 기록을 초기화하지 않는다.
  첫 플레이에 진입한 실행에서는 완료 저장 여부와 무관하게 자동 시작을 소진한다.
  결과창·일시정지에서 로비로 돌아온 뒤에는 명시적인 시작 버튼으로만 다음 판에 진입한다.
  별도 한지 로딩 화면·게임 로고·진행 막대·`게임 준비 중…` 문구와 추가 최소 대기는 없다.
  브랜드 표시 중 Main을 비동기로 준비하고 첫 UI 배치 뒤 브랜드를 0.30초에 걷는다.
  광고·원격 인증은 기다리지 않으며 실제 씬 로드 실패 때만 브랜드 아래 재시도 안내를 표시한다.
  새 설치는 브랜드가 걷히기 전에 게임 월드를 준비해 로비를 건너뛴다. 실제 게임의
  캐릭터 → 시작 발판 → 하단 먹 게이지 → 머리 위 체력을 네 단계로 비춘다.
  `TutorialSpotlightGraphic`의 78% 검정 메시에서 대상 부분만 투명하게 비우고,
  짧은 설명은 Safe Area 안에서 대상과 겹치지 않는 쪽에 둔다. 탭으로 다음 단계,
  0.26초 강조 영역 이동, 움직임 줄이기에서는 위치 전환 없이 표시한다.
  안내·닉네임 입력 동안 FirstRunTutorial 정지 사유로 물리·스폰·날씨·기록 시간을 멈춘다.
  게스트는 마지막 안내 뒤 닉네임 창 없이 완료 버전을 기록하고 같은 판을 재개한다.
  설정의 닉네임 변경 버튼·입력창은 인증된 Apple 연동 계정에만 표시한다.
  Apple 연동자는 기존 2~10자·금칙어 검사의 닉네임 설정을 유지하며,
  게스트로 전환하면 열린 이름 입력창과 키보드를 닫는다. 구멍 안 터치도 월드 입력으로 새지 않는다.
  첫 게스트 인증으로 UID가 뒤늦게 바뀌어도 입력은 보존하며, 일반 Apple 이름 설정과 중복하지 않는다.
  기존 안내 완료자는 다시 강제하지 않는다. 옵션 다시 보기는 기존 3쪽 한지 설명서로 유지하며
  한국어·영어 모두 지원한다. 토스는 기존 플랫폼 이름 정책을 유지한다.
- 옵션·일시정지·게임오버·성장 노드 상세·성장 저장 복구·첫 튜토리얼의 모달 배경은
  `InkUiStyle.PopupDimColor`의 78% 먹색을 공통 사용하고 전체 화면 Raycast를 받아
  뒤쪽 UI를 막는다. 성장 전용 화면에는 개발용 DEBUG 버튼·재화 고정·노드 초기화를
  노출하지 않는다.
- 게임 내부 팝업은 젖은 한지·작은 목축의 두루마리를 사용한다. 설정/계정 하위 선택/
  일시정지/첫 안내/성장 저장 복구는 `HanjiScrollFrame`을 공유한다. 0.28초 하강 펼침과
  작은 종이 펄럭임만 적용하며 글자·터치는 고정한다. 닫기는 아래 롤이 위로 감기는
  0.20초 역방향으로 진행하며 글자는 먼저 사라지고 dim은 마지막에 걷힌다.
  닫힘 완료 뒤에만 계속하기·첫 안내 완료·결과창 메인/광고 보상 부활을 진행한다.
  연속 닫기는 한 번만 처리하고, 다시 열기·비활성화는 남은 완료 동작을 취소한다.
  숨겨진 내용은 입력을 받지 않고
  움직임 줄이기/비활성 상태를 준수한다. Apple 로그인 및 OS·광고 SDK 시스템 창은 변형하지 않는다.
- 주요 UI는 Bold, 자동 글자 축소 금지, 충분한 터치 영역과 짧은 눌림·먹물 피드백을
  기본으로 한다. 장식용 빨간 점은 금지하고 빨강은 신기록·위험·확정 낙관과
  영구 성장에서 실제 해금된 먹나무 열매처럼 의미가 있을 때만 쓴다. 세부 규격과 공식 조사 근거는
  `docs/design/lobby-ui-overhaul-2026-07-30.md`를 따른다.
- 전체 화면 배경·붓 전환·비상호작용 VFX만 화면 끝까지 쓰고, 로비 기록/메뉴·옵션·
  성장 고정 HUD·게임 HUD·일시정지·결과창·하단 먹 게이지는 공통
  `MobileUiLayout`의 iOS/Android Safe Area 안에 둔다. 고정 패널은 좁은 논리 폭에
  맞춰 균일 축소하고 비대칭 인셋에서는 Safe Area 중심으로 이동한다. 일시정지 버튼은
  상단 띠 오른쪽 끝의 72 크기 영역에 끝이 가늘고 거친 두 먹획 아이콘만 표시한다. 한지 패널·테두리는
  없애고 최소 120 크기의 투명 터치 영역과 짧은 눌림 반응은 유지한다.
  신기록 `NEW!`는 같은 띠의 가운데 점수칸 안에서 28px 붉은 글씨로 점수 바로 위에 표시한다.
  신기록일 때만 고도 글씨를 44px의 아래 줄로 맞추고 큰 원형 받침·상시 빈 여백은 두지 않는다.
  기존 0.24초 낙관 찍기·먹 번짐을 한 번만 재생하고 이후에는 반복 없이 유지한다. 상단 안전 여백과
  일시정지 위치는 `GameplayHudView.CalculateTopHudRect`의 공통 좌표를 따른다.
- 영구 성장 지도 제목·재화처럼 상세창 밖의 핵심 HUD만 필요한 범위에서 얇은
  외곽선을 사용할 수 있다. 노드 상세창에는 합성 Bold나 외곽선을 사용하지 않는다.
- 공통 UI 구성은 회색 기하 장식을 복제하지 않고 큰 제목·넓은 여백·
  단일 주 행동만 수묵 UI로 번역한다. 옵션은 중앙 제목 아래 작은 버전,
  일시정지는 선명한 계속하기와 옅은 로비로,
  결과창은 가운데 정렬된 제목 → 이번 고도 → 최고 고도 → 먹빛 누적 게이지 → 부활·메인 버튼 순서로 읽혀야 한다.
- 성장 성공은 `GrowthBloomPresentation`의 1.4초 국소 연출을 사용한다. 0.32초간 먹방울이
  내려와 원형에 착수하면 원화가 눌렸다 복원되고, 세 겹의 납작한 수묵 파동과 금분이 퍼진다.
  DOTween 코어 1.3.030과 UGUI로 진행 먹빛이 이미 채운 단계부터 새 단계까지 흐르고,
  먹방울들이 순서대로 튀고 복원된다. 미획득 단계는 움직이지 않는다. 예전 전체 화면 암막·대각선 먹획·
  잠금/해금 문구는 현재 성장 구매에서 호출하지 않는다. 고정 UI 풀 12개를 품질별 축소하고
  입력을 받지 않으며, 움직임 줄이기에서는 확장과 금박 이동 없이 옅은 원만 페이드한다.
  `GrowthBloomSound`가 짧은 물방울 착수음·물결 잔향을 외부 샘플 없이 합성하고
  전용 AudioSource 하나를 재사용한다. 효과음 음량·음소거를 따르며 화면 이탈·앱 비활성·
  재컴파일 시 정리한다. 저장 성공 이후에만 재생하며 재화·단계 판정은 바꾸지 않는다.
- 제출 UI에는 성장·게임플레이 DEBUG 창을 노출하지 않는다. 아이템·날씨·장애물·
  고도 이동용 내부 개발 API는 자동 회귀 테스트에서만 사용한다.
- Unity Editor와 Development Build를 포함해 설정에서 `DEBUG · 연출 시나리오` 항목과
  페이지를 생성하지 않는다. 내부 촬영 API의 상황 1~5는 바람 능선 10마리 먹떼, 황금 먹비, 검은 절벽 해태 급습,
  월련 성해 천상 도약, 천하수의 50m 상승·먹분신 연쇄를 각각 다른 성장 빌드로
  시작한다. 선택값과 성장 스냅샷은
  현재 Play 세션·해당 판에만 적용하고 사용자 먹빛·노드·최고 기록은 바꾸지 않는다.
  세부 구성과 촬영 순서는 `docs/design/debug-showcase-scenarios.md`를 따른다.
- 아이템·이동 장애물·낙묵석·짧은 피드백·먹물점프 합성 VFX는 기능별 지연 풀로
  재사용하고, 먹물점프 합성 풀은 모든 분신이 게임 전체 3묶음을 공유한다. 황금 붓의
  지속 상태는 별도 월드 VFX나 아이콘 교체 없이 하단 먹 게이지 채움을 금빛으로 바꾸고,
  획득 0.7초 빛줄기·금가루 버스트 이후 작은 별빛이 게이지 주변에 흩날린다.
  `GoldenGaugeVfx`는 고정 배열·메시·머티리얼을 재사용하며 Low/Medium/High에서
  입자를 최대 24/44/64개로 제한한다. 일시정지·게임오버에는 동결/숨김,
  광고 부활에는 같은 판 상태 유지, 만료에는 0.28초 소멸, 새 판에는 즉시 비운다.
  검은 게이지 원화는 `InkGaugeTint` 셰이더가 알파·붓결을 보존하며 금색 안료로 치환한다.
  시작 0.18초·종료 0.24초 표시 보간만 사용하고 실제 먹 정산은 지연하지 않는다.
  짧은 공용 피드백은 현재 품질 예산까지, 먹물점프는 품질별 1/2/3묶음, 낙묵석은
  한 개, 사망 먹 자국은 최대 20개를 로비에서 프리웜하고 순환 재사용한다.
  결과창·화면 전환·아이템 효과·전역 바람선·구간 날씨선은 첫 사용 때 생성하며,
  디버그 고도 이동 시
  지나간 스폰 예약은 객체를 만들지 않고 건너뛴다.
- 수묵 VFX는 Low·Medium·High 모바일 소프트 예산을 적용한다. `VfxRuntimeMonitor`가
  플레이 구간의 지속 프레임 저하와 Low Memory에만 반응해 한 단계씩 낮추며,
  사망·방어막·낙묵석 경고 같은 핵심 피드백은 예약 슬롯으로 유지한다. 단계·FPS·활성 수·
  생략 수는 내부 모니터와 `MukJump > Diagnostics > Audit Mobile VFX`로
  카메라·렌더러·오디오·텍스처 예산을 읽기 전용 감사할 수 있다.
- 아이템별 획득 문양, 분신의 반대편 몸통→완성 팝, 방향성 벽 충돌, 방어막 파괴, 최고 기록 낙관,
  낙묵석 화면 하단 목표 낙관·충돌 먹 번짐을 추가했다. 같은 프레임에 몰리는 분신
  피드백은 짧게 병합하되 마지막 목숨 연출은 생략하지 않는다.
  일반 점프·착지는 실제 발밑에서 방향 붓획·납작한 먹고리를 내고, 같은 프레임의
  화면 안 접촉을 우선한다. 반복 소리를 억제하되 뒤늦은 강한 충격과 공중 재도약은 구분한다.
  픽업 효과·충돌 해제는 즉시, 원화 흡수만 기존 렌더러로 0.18초 이어서 풀로 반납한다.
  먹물방울·풍맥 합성 VFX와 카메라 임펄스는 실제 카메라 선두를 함께 사용한다.
  분신·방어막·먹물방울의 자체 효과가 있으면 일반 획득 효과를 겹치지 않는다.
  움직임 줄이기에서는 bob·파편·잔상·큰 먹기둥을 줄이며 합성 연출은 0.65초에 반납한다.
  세부 변경·검증 범위는 `docs/design/mobile-quality-polish.md`를 참고한다.
  공통 먹고리는 결을 고정한 채 가늘어지고, 방어막·상승 파편은 `InkVfxMotion`의
  프레임률 독립 감속으로 소멸한다. 빠른 공중 이동 잔향은 화면 선두 한 명에게만
  100ms·0.6m 간격으로 방출하고 기존 접촉 파티클 예산을 공유한다.
  벽 먹 튐은 벽 반대 방향으로만 방출하며, Reduced에서는 추가 잔향을 생략한다.
  `docs/design/vfx-natural-motion-pass.md`에 이번 테스트·촬영 증거를 기록한다.
- 세션 전환과 고도 순간이동은 `GameManager` 이벤트 계약으로 전달하고, 분신 표현 캐시
  제외는 `IRuntimeCloneLifecycle` 계약에 위임한다. 기능별 소유권·의존 규칙은
  `docs/architecture.md`를 기준으로 한다.
- 수묵 변환은 `SketchToInkService`와 `FallbackInkStyle`의 로컬 절차적 스타일만 사용하며
  런타임 원격 API와 API 키가 없다.
- 규칙 난수는 아이템·장애물·낙묵석·날씨·발판·플레이어별 독립 스트림으로 분리하고,
  연출 난수 호출이 게임 결과를 바꾸지 않게 한다.
- 성장 해금·UI 탭의 고정 풀은 스크립트 재컴파일 뒤 살아 있는 캔버스만 보고 초기화를
  건너뛰지 않는다. 기존 이름의 UI와 배열을 다시 연결하고 실제로 사라진 항목만 보충한다.
  재활성화 시 이전 연출은 취소하며, 재생 도중 참조가 소실되면 잔상 없이 종료한 뒤
  다음 호출에서 레이어 순서까지 복구한다. 밤 상태와 구매·재화 저장은 변경하지 않는다.
- 실제 Play 상태 물리 통합 검사를 포함한 Unity 전체 회귀 테스트를 통과했다.
  상세 감사 결과와 남은 제출 위험은
  `docs/code-audit-2026-07-26.md`를 기준으로 한다.
- 설계상 최고 고도에는 끝이 없다. 코드 규칙 100,000개 seed와 반복 향상
  50,000개×50판의 목표 퍼널 민감도 시나리오에서는 먹떼 진행 고도 1,000m 첫 도달
  중앙값이 18판이다. 70% 아이템 획득·무피격 상한 실험에서는 1,000m까지 80.83%가
  24마리에 도달했고, 도달한 세션의 고도 중앙값은 831.6m다. 선두 최고 점수와 먹떼
  진행 고도는 서로 다른 지표다. 이 수치는 실제 사용자 로그나 자동 플레이 예측값이
  아닌 입력 확률 기반 가설이며 상세 조건과 후속 검증표는
  `docs/balance-report-2026-07-27.md`를 기준으로 한다.
- `Assets/Scenes/Main.unity`는 `MukJumpSceneBuilder.cs`로만 생성하며 수동 편집하지 않는다.
- 과거 기능 브랜치의 완료 작업을 `main`에 통합했다. 2026-08-03부터 후속 작업은
  `/Users/seungyeoning/Desktop/UnityProject/muk-jump`의 `main` 하나에서 진행한다.

## 12. 리포 운영 방침

- 브랜치 전략: `main` 단일 브랜치. UI와 게임을 별도 clone/worktree로 나누지 않는다.
- 커밋 컨벤션: Conventional Commits — `feat:` `fix:` `chore:` `docs:` `art:` `build:` `refactor:`
  - 예: `feat(drawing): 터치 스트로크 캡처 및 베지어 스무딩 추가`
- 완료·검증한 작업은 이번 작업 파일만 명시적으로 stage해 바로 커밋한다.
- 대용량 바이너리(아트 원본 등)가 커지면 Git LFS 검토.
- README에 로컬 실행과 모바일 스토어 빌드 방법을 최신 상태로 유지한다.

## 13. 제작 자료와 라이선스

작업 과정 기록은 필수가 아니다. 기존 제작 기록은 참고 자료로만 보존한다. 외부 에셋과
오픈소스의 출처·라이선스·재배포 조건은 실제 스토어 출시 전에 반드시 확인한다.

## 14. 개발 로드맵

### 완료 — 코어 루프와 콘텐츠
- 세로 화면, 상승 카메라, 자동 점프, 드로잉 발판, 고도 점수, 재도전
- 캐릭터 8프레임 애니메이션, 장애물 4종, 아이템 4종, 최대 24마리 먹분신
- 로비·게임 HUD, 수묵 전환, 먹물방울 VFX/SFX, 먹 번짐 사망 자국

### 진행 — 폴리시와 검증
- 실제 모바일 해상도에서 UI·아이템 크기·가독성 검증
- 분신 동시 사망, 방패 중복 충돌, 장애물과 발판 조합 회귀 테스트
- 프레임 드롭과 장시간 플레이 시 풀 보존 상한·활성 오브젝트 수 프로파일링

### 출시 준비
- iOS Xcode Archive와 TestFlight 실기기 테스트
- Android App Bundle 서명과 내부 테스트
- Apps in Toss WebGL `.ait` 패키지와 토스앱 QR 테스트
- AdMob 먹점프 앱·배너·보상형 단위 생성, UMP 메시지 게시와 iOS·Android 실기기 검증
- App Store Connect·Google Play 메타데이터, 개인정보처리방침, 스크린샷 준비
- 라이선스·크래시·성능·저장 데이터 최종 점검

## 15. 결정 필요 사항

1. Apple Developer Team ID `8AU359WZZ2`와 App Store Connect 앱 레코드 생성
2. iOS용 1024×1024 앱 아이콘과 스토어 스크린샷 최종본
3. Android keystore와 iOS 인증서·프로비저닝 관리 방식
4. 개인정보처리방침과 고객지원 URL 공개 위치
5. 새 외부 에셋을 추가할 때 `THIRD_PARTY_NOTICES.md`와 원문 라이선스를 함께 갱신

## 16. 스토어 산출물

| 플랫폼 | 산출물 |
|---|---|
| iOS | 서명된 Archive · App Store Connect 빌드 · TestFlight 검증 |
| Android | 서명된 AAB · Play Console 내부 테스트 |
| Apps in Toss | WebGL `.ait` · 광고 그룹 ID · 토스앱 QR 검증 |
| 공통 | 아이콘 · 스크린샷 · 설명 · 지원 URL · 개인정보처리방침 · 등급 정보 |

## 17. 다음 작업 우선순위

1. 여러 테스터가 각 15~30판 기록한 고도·시간·사망 원인·분신 수·아이템 로그로
   `docs/balance-report-2026-07-27.md`의 1,000m 도달 가설 보정
2. 아이템 4종과 특수 발판·무적·고도 이동 내부 API의 자동 회귀 테스트
3. 다중 먹분신의 카메라·점수·동시 사망·재획득 흐름 검증
4. 장시간 플레이에서 풀 보존 상한·활성 객체 수를 프로파일링하고 iOS·Android 실기기
   성능·입력·백그라운드 복귀를 테스트
5. iOS TestFlight와 Google Play 내부 테스트 배포
6. 스토어 메타데이터와 개인정보처리방침 완성
